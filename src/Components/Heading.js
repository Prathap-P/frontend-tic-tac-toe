import React from 'react';
import {BrowserRouter, Link} from 'react-router-dom';

const Heading= (props) => {
    return(
        <h1 class= "heading">
            Tic-Tac-Toe
        </h1>
    );
}

export default Heading;
